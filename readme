This project provides the command `diff` which takes in two strings
 and compares them, returning information about their differences.

It is intended to be used on the same string before and after a 
change has been made, to highlight the change.
